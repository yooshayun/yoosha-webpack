import Cookies from 'js-cookie';

const user = {
    state: {
        token: '' // 登录的token
    },
    mutations: {
        LOG_IN(state, info) {
            
        },
        LOG_OUT(state, vm) {
            
        }
    },
    actions: {
        LogIn({ commit }, data) {
            
        }
    }
};

export default user;